'use strict';

/**
 * Barrel export for the fleet bounded context.
 * Populate as entities, value objects, domain services and domain
 * events are implemented here — see domain/inventory for a worked
 * example of the pattern.
 */
module.exports = {};

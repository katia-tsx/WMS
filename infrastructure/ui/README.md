# UI adapter

Home for the browser-facing driving adapter — the `index.html` / `css/` / `js/` frontend described in the root [README.md](../../README.md) — once it is wired to call into `infrastructure/http/` or `infrastructure/di/CompositionRoot.js` instead of holding business logic itself.
